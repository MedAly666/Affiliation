// place files you want to import through the `$lib` alias in this folder.
export type Product = {
    id: number;
    title: string;
    price: number;
    original_price: number;
    image: string;
    url: string;
    url_hash: string;
    created_at: string;
};